apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: stonksco
data:
  JWT_ALGORITHM: "HS256"
  ACCESS_TOKEN_EXPIRE_MINUTES: "15"
  REFRESH_TOKEN_EXPIRE_DAYS: "7"
  ENV: "development"
